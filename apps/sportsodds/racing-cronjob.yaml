apiVersion: batch/v1
kind: CronJob
metadata:
  name: sportsodds-racing
  namespace: sportsodds
spec:
  schedule: "*/5 6-22 * * *"
  timeZone: "Australia/Brisbane"
  startingDeadlineSeconds: 120
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      activeDeadlineSeconds: 120
      template:
        spec:
          restartPolicy: Never
          imagePullSecrets:
            - name: gitlab-registry-credentials
          containers:
            - name: sportsodds-racing
<<<<<<< HEAD
              image: registry.taxayp.com/taxayp/sportsodds:1289687b
=======
              # IMPORTANT: keep this image tag in sync with the web Deployment.
              # Same image serves web + all cron jobs; it must contain
              # racingFetch.js / db-racing.js / racingBookies.js.
              image: registry.taxayp.com/taxayp/sportsodds:7dbb623b
>>>>>>> bbeb94913fce39a1d7b92eade4935ced584db92f
              command: ["node", "racingFetch.js"]
              env:
                - name: DATABASE_URL
                  valueFrom:
                    secretKeyRef:
                      name: sportsodds-pg-app
                      key: uri
              envFrom:
                - secretRef:
                    name: sportsodds-secrets
              resources:
                requests:
                  cpu: 50m
                  memory: 128Mi
                limits:
                  cpu: 300m
                  memory: 256Mi